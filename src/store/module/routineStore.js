import { RoutineApi } from "../../../api/routine";

const routineStore = {
    routineAux,

    async addRoutine(tempRoutine) {
        let routine = {
            name: tempRoutine.name,
            detail: tempRoutine.detail,
            isPublic: tempRoutine.isPublic,
            metadata: null
        }
    }
}